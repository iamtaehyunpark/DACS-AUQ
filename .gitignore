__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/

# experiment outputs (regenerated; never hand-edited)
data/trajectories/
data/labels/
data/analysis/
results/
runs/
*.log

# local scratch
scratch/
.DS_Store

# local inspection doc — not published (contains infra details)
HANDOFF.md

# upstream ReAct clone — reference-only, never imported/edited (build-fresh policy)
react_validation/ReAct/

# npm / claude-code artifacts — not part of the research repo
node_modules/
.claude/

# pilot run artifacts (raw thread dumps / logs) — not committed

# manual probe scratch prompt

# react_validation reorganized layout — data/logs/generated-viz are gitignored, code is tracked
react_validation/data/
react_validation/runs/
react_validation/viz/*.html
react_validation/**/__pycache__/
react_validation/**/_prompt.txt

# ---- heavy experiment outputs: kept on the server, NEVER pushed --------------
# react_validation/result/ is ~34G of raw run logs, probe records and judge output;
# v1/data/ is ~326M. The generated tables/figures that summarise them ARE tracked
# (react_validation/reports/, react_validation/figures/).
react_validation/result/
v1/data/
react_validation/analysis/*.jsonl
react_validation/src/*.jsonl
*.jsonl.gz

# per-run scratch state (regenerated by the harness, meaningless off-machine)
react_validation/src/.last_parallel_seed
react_validation/rerun_tasks_llama.txt
react_validation/**/*.w[0-9].jsonl

# pre-"expert update" safety copy of the four uncommitted src files; superseded
# now that those edits are committed
.server_backup_preexpert/
